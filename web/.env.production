# Cloudflare Turnstile — PUBLIC site key (safe to commit; the matching secret
# lives only in the box .env). Enables the bot gate on the map agent + RAG chat:
# with this set, turnstileToken() mints a token the backend verifies. See
# web/src/scripts/turnstile.ts and the harness security/turnstile.py.
PUBLIC_TURNSTILE_SITEKEY=0x4AAAAAAEXL202FsczLfA2l
