# Dependencies needed to import the `riparian` package + run the unit-test suite in CI.
# Deliberately excludes the heavy/unused-at-test-time deps (torch, olmoearth-pretrain-minimal,
# geopandas, geoalchemy2, apscheduler) so CI stays fast. The `live`-marked tests (STAC/DB)
# are skipped in CI, so their runtime deps aren't required here.
numpy>=1.26.0
xarray>=2024.1.0
scipy>=1.11.0
scikit-learn>=1.3.0
shapely>=2.0.0
rasterio>=1.3.0
odc-stac>=0.3.9
planetary-computer>=1.0.0
pystac-client>=0.8.0
sqlalchemy>=2.0.0
requests>=2.31.0
