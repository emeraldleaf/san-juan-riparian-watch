
# IDE & tooling
.github/instructions/codacy.instructions.md

# Dev script state
.aspire.pid

# Build outputs
**/bin/
**/obj/
**/dist/
**/node_modules/

# Secrets & environment
.env
*.user

# PostgreSQL data (bind-mounted, survives Docker restarts)
pgdata/

# Database backups
backups/

# Encoding-loop skill local envs (recreated via uv sync)
.claude/skills/**/.venv/
.claude/skills/**/__pycache__/

# Python bytecode (general)
__pycache__/
*.pyc

# SonarQube scanner output (regenerated each scan)
.sonarqube/

# macOS
.DS_Store
.venv-olmoearth/
experiments/riparian_extent/dataset/
experiments/riparian_extent/dataset_*/
.tmp/
docs/audits/corpus_pdfs/
docs/audits/fetch_failures.csv
docs/audits/closed_access.csv
docs/audits/fetch.log
experiments/riparian_extent/naip_chips/
.codacy/
# Derived FM experiment data — rslearn datasets + materialized reach cubes
# (69GB, 156k files, regenerable via materialize_reach.py). Never commit.
# (Blanket ignore — subsumes the specific experiments/riparian_extent/* rules above.)
experiments/
